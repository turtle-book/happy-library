// import

function BookInfo({ id, title, author, year, imageURL }) {
  return (
    <>
      <div>{image}</div>
      <div>{title}</div>
      <div>{author}</div>
      <div>{year}</div>
    </>
  );
}

export default BookInfo;